/* Deprecated: GA4 config is now inline in HTML <head>.
   Kept as a no-op stub so any cached page referencing /js/ga4.js does not error.
   Measurement ID G-FTWBPG8BJJ is configured via the standard gtag inline snippet. */
